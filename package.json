{
  "name": "python",
  "version": "1.0.0",
  "description": "proyect Chatbot",
  "scripts": {
    "install": "npm run venv:create && npm run venv:activate && npm run install:dependencies",
    "venv:create": "python3 -m venv modules",
    "venv:activate": "source modules/bin/activate",
    "venv:deactivate": "deactivate",
    "venv:remove": "$ rm -rf modules",
    "install:dependencies": "pip install -r requirements.txt",
    "startFile": "python3 app/chatbot_api.py",
    "start": "uvicorn main:app --reload",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
